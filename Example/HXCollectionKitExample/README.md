# HXCollectionKitExample

UIKit example app skeleton for HXCollectionKit.

This app intentionally avoids Storyboards and wires the scene manually through
`AppDelegate`, `SceneDelegate`, and `RootViewController`.
